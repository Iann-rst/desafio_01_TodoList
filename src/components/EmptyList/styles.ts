import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    marginTop: 48
  },
  image: {
    marginBottom: 16
  },

  subTitle: {
    color: '#808080',
    fontSize: 14,
    textAlign: 'center'
  },

  strong: {
    fontWeight: 'bold'
  }
})